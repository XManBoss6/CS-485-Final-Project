# CS-485-Final-Project

To run the program, please enter the following commands in the terminal (you may need to install "node.js"):

> npm run start

> npm run build

> npm run serve

then open the forwarded port

to run the unit test (you may need to install "jest"):
> npm run test